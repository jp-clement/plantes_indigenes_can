# experiment configuration
exp_name: "gpt52-RAG"

# output settings
dpi: 300
show_plot: false
show_imp_plot: true

# dimensionality reduction settings
reduce_dim: True
reduce_dim_method: "svd"   # options: pca, svd
n_dim: 50
idf: false

scree_max_dim: 40

# paths
notebook_dir: "."
run_dir: "runs"
output_dir: "output"